# 🔗 КАРТА ЗАВИСИМОСТЕЙ

**Дата:** 21 июня 2026  
**Статус:** Полная карта всех связей между сущностями  

---

## 🌐 ГРАФ ЗАВИСИМОСТЕЙ

```
┌──────────────────────────────────────────────────────────────┐
│                    ГЛОБАЛЬНАЯ ЗАВИСИМОСТЬ                    │
│                                                               │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  makeColumned Nomenclature (App.jsx)                │   │
│   │  = Товары + Полуфабрикаты                           │   │
│   │  (используется везде для datalist)                  │   │
│   └──────────────┬──────────────────────────────────────┘   │
│                  │                                            │
│      ┌───────────┼───────────┬──────────────┬──────────┐    │
│      ▼           ▼           ▼              ▼          ▼     │
│  ┌───────┐  ┌─────────┐  ┌──────┐  ┌──────────┐  ┌────────┐
│  │Товары │  │ П/Ф    │  │Блюда │  │Коллекции │  │Произво-│
│  │(local)│  │(local) │  │(local)│  │ (local)  │  │дство   │
│  └───┬───┘  └────┬────┘  └──┬───┘  └────┬─────┘  │(local) │
│      │           │          │           │        └────────┘
│      └───────────┼──────────┼───────────┘
│                  │          │
│    ┌─────────────┴──────────┴──────────┐
│    ▼                                    ▼
│  ┌─────────────────┐        ┌──────────────┐
│  │ Ингредиенты    │        │ dishIds[]    │
│  │ rows[] (ссылки) │        │ (orphans?)   │
│  └─────────────────┘        └──────────────┘
│
└──────────────────────────────────────────────────────────────┘
```

---

## 📍 ДЕТАЛЬНАЯ КАРТА

### Уровень 1: СУЩНОСТИ

```
ТОВАРЫ (Products)
├─ localStorage: klevo_products
├─ Хук: useProducts()
├─ Где хранит: Array<Product>
├─ Где используется:
│  ├─ ReferenceTtk.jsx (ингредиенты, datalist)
│  ├─ SemifinishedPage.jsx (ингредиенты, datalist)
│  └─ Nomenclature.jsx (объединённый справочник)
├─ Кто удаляет:
│  └─ ProductsPage.jsx (onDelete)
└─ ⚠️ ПРОБЛЕМА: Нет проверки при удалении

ПОЛУФАБРИКАТЫ (Semifinished)
├─ localStorage: klevo_semifinished
├─ Хук: useSemifinished()
├─ Где используется:
│  ├─ ReferenceTtk.jsx (ингредиенты, datalist)
│  ├─ SemifinishedPage.jsx (рекурсивно в rows)
│  └─ Nomenclature.jsx (объединённый справочник)
├─ Кто удаляет:
│  └─ SemifinishedPage.jsx (onDelete)
└─ ⚠️ ПРОБЛЕМА: Нет проверки + циклические зависимости

БЛЮДА / ТТК (ReferenceTtk)
├─ localStorage: academy_printable_reference_ttk_v1
├─ Supabase: reference_ttk (+ photos storage)
├─ Хук: useReferenceTtk()
├─ Где используется:
│  ├─ ReferenceTtk.jsx (основной редактор)
│  ├─ MenuArchitectPage.jsx (AI Menu Engineering)
│  ├─ Collections.js (dishIds references)
│  └─ Production (может использовать)
├─ Кто удаляет:
│  └─ ReferenceTtk.jsx (onDelete)
└─ ⚠️ ПРОБЛЕМА: Orphan dishIds в коллекциях

КОЛЛЕКЦИИ (Collections)
├─ localStorage: chefcloud_collections_v1
├─ Хук: useCollections()
├─ Содержит: Array<Collection> с dishIds[]
├─ Системные: Favorites, Cookbook
├─ Где используется:
│  └─ ReferenceTtk.jsx (сайдбар)
└─ ⚠️ ПРОБЛЕМА: Нет очистки orphan dishIds

ПРОИЗВОДСТВО (Production)
├─ localStorage: chefcloud_production_tasks
├─ Хук: ❌ НЕТ (только readTasks/writeTasks)
├─ Содержит: Array<Task> (не сохраняются)
├─ Данные теряются при F5: ✅ ДА
└─ ❌ КРИТИЧНАЯ ПРОБЛЕМА: Не интегрировано в систему
```

---

## 🔀 ТОЧКИ СОЕДИНЕНИЯ

### Соединение 1: Ингредиенты в Блюдах

```
ReferenceTtk.jsx (TTK Form)
    │
    ├─→ datalist с items из makeCombinedNomenclature()
    │   ├─ Товары из useProducts()
    │   └─ ПФ из useSemifinished()
    │
    ├─→ При сохранении блюда:
    │   └─ rows[] содержит { name, type, qty, unit }
    │       (только имена, ID не сохраняется!)
    │
    └─→ ⚠️ ПРОБЛЕМА: Нет связи по ID
        Если переименовать товар → ссылка потеряется
        Если удалить товар → ссылка остаётся (мёртвая)
```

### Соединение 2: Ингредиенты в ПФ

```
SemifinishedPage.jsx (SemiForm)
    │
    ├─→ datalist из makeCombinedNomenclature()
    │   ├─ Товары
    │   └─ ПФ (рекурсивно!)
    │
    ├─→ При сохранении ПФ:
    │   ├─ rows[] (ингредиенты)
    │   └─ developmentIngredients[] (факт)
    │       (также только имена)
    │
    └─→ ⚠️ ПРОБЛЕМА: 
        ├─ Циклические зависимости (ПФ→ПФ→ПФ)
        └─ Нет проверки на циклы
```

### Соединение 3: Блюда ↔ Коллекции

```
ReferenceTtk.jsx → Collections
    │
    ├─→ Блюдо добавляется в коллекцию
    │   └─ Сохраняется dishId в collection.dishIds[]
    │
    ├─→ При удалении блюда:
    │   ❌ dishId НЕ удаляется из коллекции
    │   └─ Orphan: коллекция содержит мёртвый ID
    │
    └─→ ⚠️ КРИТИЧНАЯ ПРОБЛЕМА:
        При открытии коллекции → ошибка,
        так как блюдо не найдено
```

### Соединение 4: ПФ → Товары (auto-creation)

```
SemifinishedPage.jsx: ensureIngredientsExist()
    │
    ├─→ Проверяет каждый ингредиент в rows
    │
    ├─→ Если type='product' и товара нет:
    │   └─ Автоматически создаёт товар
    │       (saveProduct из App.jsx)
    │
    ├─→ Если type='semifinished' и ПФ нет:
    │   └─ Автоматически создаёт ПФ-заглушку
    │       (saveSemifinished из App.jsx)
    │
    └─→ ✅ ЭТО РАБОТАЕТ, но может быть улучшено
```

---

## 🔄 FLOW: УДАЛЕНИЕ СУЩНОСТИ

### Flow: Удаление товара

```
ProductsPage.jsx
    │
    ├─→ confirmDelete(productId)
    │
    ├─→ onDelete(productId)  [App.jsx]
    │
    ├─→ products.deleteItem(productId)  [useProducts hook]
    │
    ├─→ localStorage.setItem(PRODUCTS_KEY, ...)
    │
    └─→ ❌ ВСЁ! Нет проверок, товар удалён!

ПОСЛЕДСТВИЯ:
├─ Блюда, которые использовали этот товар → broken ingredient
├─ ПФ, которые использовали этот товар → broken ingredient
└─ Система ломается, но молча!
```

### Flow: Удаление блюда

```
ReferenceTtk.jsx
    │
    ├─→ onDelete(ttk.id)  [App.jsx]
    │
    ├─→ ttk.deleteTtk(id)  [useReferenceTtk hook]
    │
    ├─→ localStorage.setItem(TTK_KEY, ...)
    │
    └─→ ❌ Готово, но...
        
ПОСЛЕДСТВИЯ:
├─ Collections всё ещё содержат dishIds
└─ Коллекции становятся broken!
    (при попытке открыть коллекцию → ошибка)
```

---

## ⚡ ТОЧКИ РИСКА И ЦИКЛИЧЕСКИЕ ЗАВИСИМОСТИ

### Risk Point 1: Circular Dependencies в ПФ

```
ПФ1 (содержит ПФ2)
 └─→ ПФ2 (содержит ПФ3)
      └─→ ПФ3 (содержит ПФ1) ← ЦИКЛ!

Это может привести к:
├─ Бесконечной рекурсии при обработке ингредиентов
├─ Проблемам при печати
└─ Проблемам при экспорте/импорте
```

### Risk Point 2: Orphan Dishes в коллекциях

```
Collection.dishIds = ["ttk_1", "ttk_2", "ttk_deleted"]

При открытии коллекции:
├─ items.filter(i => collection.dishIds.includes(i.id))
├─ ttk_1, ttk_2 найдены ✅
├─ ttk_deleted НЕ найден ✅
└─ Но коллекция "грязная" — может вызвать проблемы

При экспорте коллекции:
└─ В JSON попадёт мёртвый ID → проблемы при импорте
```

### Risk Point 3: Base64 фото в localStorage

```
Photo stored as base64:
├─ Size: 1-5 МБ на одно фото
├─ localStorage limit: 5-10 МБ на сайт
└─ Риск: Переполнение localStorage!
    └─→ Новые данные не сохраняются!

Scenario:
1. Пользователь загружает большое фото
2. photo.dataUrl ~5 МБ
3. localStorage переполнен
4. Другие хуки не могут сохранить
5. Система ломается молча!
```

---

## 📊 МАТРИЦА ИСПОЛЬЗОВАНИЯ

| Сущность | Товары | П/Ф | Блюда | Коллекции | Производство |
|----------|--------|-----|-------|-----------|--------------|
| **Товары** | — | ✅ ингр | ✅ ингр | — | — |
| **П/Ф** | — | ✅ рекур | ✅ ингр | — | — |
| **Блюда** | — | — | — | ✅ dishIds | ❌ нет |
| **Коллекции** | — | — | ✅ refs | — | — |
| **Производство** | — | — | ❌ нет | — | — |

**Легенда:**
- ✅ Используется
- ❌ Не используется (проблема)
- ✅ рекур — рекурсивно (опасно)
- ✅ ингр — как ингредиент
- ✅ refs — references
- ✅ dishIds — массив ID

---

## 🎯 КРИТИЧНЫЕ ЗАВИСИМОСТИ

### Tier 1: КРИТИЧНЫЕ (нужно исправить перед MVP)

1. **Товары → Блюда/ПФ**
   - Удаление товара → мёртвые ссылки
   - Проверка: список всех используемых товаров
   - Решение: Запретить удаление, если используется

2. **ПФ → Блюда/ПФ**
   - Удаление ПФ → мёртвые ссылки
   - Циклические зависимости ПФ→ПФ
   - Решение: Запретить удаление, проверка циклов

3. **Блюда → Коллекции**
   - Удаление блюда → orphan dishIds
   - Коллекция содержит мёртвый ID
   - Решение: Удалить ID из коллекций при удалении

### Tier 2: ВЫСОКИЕ (нужно исправить скоро)

4. **Товары/ПФ → Nomenclature (комбинированная)**
   - Nomenclature строится на лету
   - Если товар удалён → может вызвать ошибку
   - Решение: Валидация при построении

5. **Фото → localStorage**
   - Base64 фото переполняет хранилище
   - Другие данные не сохраняются
   - Решение: Миграция на Supabase Storage или лимит

### Tier 3: СРЕДНИЕ (планы на v1.1)

6. **Производство ↔ все сущности**
   - Производство не интегрировано
   - Нельзя импортировать план из ТТК
   - Решение: Создать useProduction() hook, интеграция

---

## 🚨 ТАБЛИЦА РИСКА

| Операция | До исправления | Після исправления |
|----------|---|---|
| Удалить товар, используемый в 5 блюдах | ❌ Ломается 5 блюд | ✅ Запрещено, показано сообщение |
| Удалить блюдо из коллекции | ❌ Orphan ID | ✅ Автоматически удаляется из коллекции |
| Удалить ПФ, используемый в 3 ПФ | ❌ 3 ПФ ломаются | ✅ Запрещено, показано сообщение |
| Загрузить большое фото | ❌ Переполнение | ✅ Лимит + предупреждение |
| Импортировать план в Производство | ❌ Нет такой функции | ✅ Кнопка + диалог выбора |

---

## 🔧 РЕШЕНИЯ ПО ПРИОРИТЕТАМ

### Phase 1: Защита целостности (СРОЧНО)

```javascript
// Перед удалением товара:
checkProductUsage(productId) {
  const inDishes = items.filter(ttk => 
    ttk.rows.some(r => r.name === productName)
  )
  const inSemifinished = semifinished.filter(pf =>
    pf.rows.some(r => r.name === productName)
  )
  
  if (inDishes.length > 0 || inSemifinished.length > 0) {
    return {
      canDelete: false,
      message: `Товар используется в ${inDishes.length} блюдах и ${inSemifinished.length} ПФ`
    }
  }
  return { canDelete: true }
}

// Аналогично для ПФ и блюд
```

### Phase 2: Очистка orphans

```javascript
// При удалении блюда:
deleteDish(dishId) {
  // 1. Удалить само блюдо
  items.deleteItem(dishId)
  
  // 2. Очистить из всех коллекций
  collections.forEach(col => {
    col.dishIds = col.dishIds.filter(id => id !== dishId)
  })
  
  // 3. Сохранить коллекции
  collectionStore.persist()
}
```

---

## 📈 ВЫВОДЫ

**ChefCloud зависимости:**
- ✅ Логичная структура
- ✅ Хорошая модульность
- ❌ Отсутствует проверка целостности
- ❌ Orphan данные нестабильны
- ⚠️ Циклические зависимости в ПФ не проверяются

**MVP готовность:** 65% (нужна стабилизация)

